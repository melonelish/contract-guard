# 接口设计

> 版本：v1.0 | 最后更新：2026-06-15

---

## 一、API 设计原则

- RESTful 风格
- 统一 JSON 响应格式
- 分页统一使用 `page` + `page_size`
- 多租户隔离：所有接口自动注入 `tenant_id`

---

## 二、统一响应格式

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "meta": {
    "page": 1,
    "page_size": 20,
    "total": 100
  }
}
```

---

## 三、核心 API

### 3.1 用户模块

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/v1/auth/register` | 注册 |
| POST | `/api/v1/auth/login` | 登录（返回 JWT） |
| GET | `/api/v1/users/me` | 获取当前用户 |
| PUT | `/api/v1/users/me` | 更新个人信息 |

### 3.2 合同模块

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/v1/contracts/upload` | 上传合同（multipart/form-data） |
| GET | `/api/v1/contracts` | 合同列表（分页） |
| GET | `/api/v1/contracts/{id}` | 合同详情 |
| DELETE | `/api/v1/contracts/{id}` | 删除合同 |

### 3.3 审查模块

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/v1/contracts/{id}/review` | 触发审查 |
| GET | `/api/v1/reviews/{id}` | 获取审查报告 |
| GET | `/api/v1/reviews/{id}/status` | 查询审查进度 |
| GET | `/api/v1/reviews/{id}/export?format=pdf` | 导出报告 |

### 3.4 团队模块

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/v1/teams` | 创建团队 |
| GET | `/api/v1/teams/{id}` | 团队信息 |
| POST | `/api/v1/teams/{id}/members` | 邀请成员 |

### 3.5 编辑模块

| 方法 | 路径 | 说明 |
|---|---|---|
| PUT | `/api/v1/contracts/{id}/draft` | 保存编辑草稿 |
| POST | `/api/v1/contracts/{id}/draft/review` | 重新审查 |
| GET | `/api/v1/contracts/{id}/diff` | 差异对比 |

### 3.6 版本与差异

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/v1/contracts/{id}/versions` | 版本历史列表 |
| GET | `/api/v1/contracts/{id}/diffs?from=v1&to=v2` | 版本间差异对比 |

### 3.7 反馈模块

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/v1/reviews/{id}/feedback` | 提交审查反馈（准确/不准确） |

### 3.8 管理后台

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/v1/admin/kb/versions` | 知识库版本历史 |

### 3.9 WebSocket

| 端点 | 说明 |
|---|---|
| `ws://host/ws/review/{task_id}` | 审查进度实时推送 |

---

## 四、接口详情

### 4.1 上传合同

```
POST /api/v1/contracts/upload

Headers:
  Authorization: Bearer {jwt_token}
  Content-Type: multipart/form-data

Body:
  file: (binary)     # PDF/Word/图片文件
  title: "采购合同"   # 可选，不填自动提取

Response 201:
{
  "code": 0,
  "data": {
    "id": "ct-abc123",
    "title": "采购合同",
    "file_url": "https://cdn.xxx.com/contracts/abc123.pdf",
    "status": "uploaded",
    "created_at": "2026-06-15T10:00:00Z"
  }
}
```

### 4.2 触发审查

```
POST /api/v1/contracts/{id}/review

Response 202:
{
  "code": 0,
  "data": {
    "review_id": "rv-xyz789",
    "status": "queued",
    "estimated_seconds": 900
  }
}
```

### 4.3 WebSocket 推送

```
ws://host/ws/review/task-xyz789

服务端推送事件:
1. {"event": "status_change", "status": "parsing", "progress": 10}
2. {"event": "status_change", "status": "analyzing", "progress": 50}
3. {"event": "status_change", "status": "generating_report", "progress": 80}
4. {"event": "status_change", "status": "completed", "progress": 100}
5. {"event": "review_complete", "review_id": "rv-xyz789", "summary": {...}}
```

### 4.4 获取审查报告

```
GET /api/v1/reviews/{id}

Response 200:
{
  "code": 0,
  "data": {
    "id": "rv-xyz789",
    "contract": { "title": "采购合同", ... },
    "summary": {
      "total_risks": 12,
      "high": 3, "medium": 5, "low": 4
    },
    "risks": [
      {
        "clause_id": "cl_007",
        "risk_level": "high",
        "risk_category": "违约责任",
        "original_text": "如乙方逾期交货...",
        "legal_analysis": "...",
        "law_references": [...],
        "case_references": [...],
        "plain_explanation": "...",
        "suggested_revision": "...",
        "confidence": 0.92
      }
    ],
    "contradictions": [...],
    "missing_clauses": [...]
  }
}
```

### 4.5 健康检查 (K8s Liveness/Readiness)

```
GET /api/v1/health

Response 200:
{
  "status": "healthy",
  "version": "1.0.0",
  "uptime_seconds": 86400,
  "checks": {
    "postgres": "ok",
    "redis": "ok",
    "elasticsearch": "ok",
    "milvus": "ok"
  }
}

GET /api/v1/health/ready

Response 200:
{
  "status": "ready",
  "llm_models": ["mimo2.5", "deepseek-v4-flash"],
  "rag_ready": true
}
```

### 4.6 团队管理

```
POST   /api/v1/teams             创建团队
GET    /api/v1/teams/{id}        团队信息
POST   /api/v1/teams/{id}/members 邀请成员
DELETE /api/v1/teams/{id}/members/{uid} 移除成员
GET    /api/v1/teams/{id}/members 成员列表
PUT    /api/v1/teams/{id}/members/{uid}/role 修改成员角色
```

### 4.7 合同编辑（PRD S04/S05）

```
PUT    /api/v1/contracts/{id}/draft       保存编辑草稿
POST   /api/v1/contracts/{id}/draft/review 对编辑后合同重新审查
GET    /api/v1/contracts/{id}/diff         获取修改前后差异对比
```

### 4.8 合同版本历史

```
GET /api/v1/contracts/{id}/versions

Response 200:
{
  "code": 0,
  "data": {
    "contract_id": "ct-abc123",
    "versions": [
      {
        "version_number": 3,
        "created_by": "user-uuid",
        "created_at": "2026-06-15T14:30:00Z",
        "clauses_added": 2,
        "clauses_removed": 0,
        "clauses_modified": 5
      },
      {
        "version_number": 2,
        "created_by": "user-uuid",
        "created_at": "2026-06-15T12:00:00Z",
        "clauses_added": 0,
        "clauses_removed": 1,
        "clauses_modified": 3
      }
    ]
  }
}
```

### 4.9 版本差异对比

```
GET /api/v1/contracts/{id}/diffs?from=v1&to=v2

Response 200:
{
  "code": 0,
  "data": {
    "from_version": 1,
    "to_version": 2,
    "clauses_added": [
      { "title": "保密条款", "full_text": "..." }
    ],
    "clauses_removed": [],
    "clauses_modified": [
      {
        "clause_id": "cl_007",
        "title": "违约责任",
        "old_text": "如乙方逾期交货...",
        "new_text": "如乙方逾期交货超过30日..."
      }
    ]
  }
}
```

### 4.10 审查反馈

```
POST /api/v1/reviews/{id}/feedback

Headers:
  Authorization: Bearer {jwt_token}
  Content-Type: application/json

Body:
{
  "clause_analysis_id": "ca-uuid",
  "vote": "accurate",
  "comment": "分析准确，引用法规恰当"
}

Response 201:
{
  "code": 0,
  "data": {
    "id": "fb-uuid",
    "vote": "accurate",
    "created_at": "2026-06-15T15:00:00Z"
  }
}
```

### 4.11 知识库版本管理

```
GET /api/v1/admin/kb/versions

Response 200:
{
  "code": 0,
  "data": {
    "versions": [
      {
        "version": "v2.1.0",
        "release_date": "2026-06-01",
        "changes": [
          "新增《民法典合同编司法解释》全文",
          "更新判例库至2026年5月"
        ],
        "law_count": 12500,
        "case_count": 350000
      }
    ]
  }
}
```
